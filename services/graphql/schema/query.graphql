"""
Root query type
"""
type Query {
  """
  Query to make sure that the server is up and running, will
  always return hello world
  """
  hello: String

  """
  Returns the logged in user
  """
  currentUser: User
}
